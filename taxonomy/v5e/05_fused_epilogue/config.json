{
  "cell_name": "fused_epilogue",
  "task_description": "relu(A @ B + bias) with A (2048,1024) and B (1024,1024) in bf16, row-blocked: two separate pallas_calls (8 MB float32 intermediate through HBM) versus one fused pallas_call.",
  "profiling": {
    "metrics": [
      {
        "name": "num_pallas_calls",
        "description": "Number of pallas_call invocations in workload(). The intermediate matmul result round-trips through HBM between separate calls.",
        "direction": "lower_is_better",
        "expected_naive": 2,
        "expected_optimized": 1
      },
      {
        "name": "achieved_tflops_pct_of_peak_bf16",
        "description": "Achieved TFLOPS as a percentage of v5e peak bf16 TFLOPS (197.0). Secondary signal.",
        "direction": "higher_is_better"
      }
    ]
  }
}
