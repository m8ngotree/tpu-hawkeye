{
  "cell_name": "mxu_feed",
  "task_description": "128x128x128 bf16 matmul in a Pallas TPU kernel: jnp.dot (MXU) versus an equivalent broadcast-multiply and sum (VPU only).",
  "profiling": {
    "metrics": [
      {
        "name": "achieved_tflops_pct_of_peak_bf16",
        "description": "Achieved TFLOPS as a percentage of v5e peak bf16 TFLOPS (197.0): measured_tflops / peak_tflops_bf16 * 100.",
        "direction": "higher_is_better",
        "expected_naive": "near 0% (computation runs entirely on the VPU)",
        "expected_optimized": "substantially higher than naive; a single 128x128x128 block is too small to approach peak"
      }
    ]
  }
}
