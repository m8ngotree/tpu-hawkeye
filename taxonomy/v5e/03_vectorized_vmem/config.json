{
  "cell_name": "vectorized_vmem",
  "task_description": "Elementwise y = x*scale + bias over a 128x128 bf16 array: per-lane Python loop (N single-lane VMEM accesses) versus one whole-block statement.",
  "profiling": {
    "metrics": [
      {
        "name": "num_vmem_ops",
        "description": "Number of distinct VMEM read/write operations issued by the kernel body per block.",
        "direction": "lower_is_better",
        "expected_naive": 128,
        "expected_optimized": 1
      },
      {
        "name": "achieved_tflops_pct_of_peak_bf16",
        "description": "Achieved TFLOPS as a percentage of v5e peak bf16 TFLOPS (197.0). Secondary signal; this kernel is not compute-bound.",
        "direction": "higher_is_better"
      }
    ]
  }
}
