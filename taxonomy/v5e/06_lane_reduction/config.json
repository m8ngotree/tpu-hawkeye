{
  "cell_name": "lane_reduction",
  "task_description": "Row-sum of a 128x128 bf16 array: per-lane Python accumulation loop (128 single-lane reads and adds) versus one jnp.sum(axis=1).",
  "profiling": {
    "metrics": [
      {
        "name": "num_reduce_ops",
        "description": "Number of distinct reduction-related VMEM read/accumulate operations issued.",
        "direction": "lower_is_better",
        "expected_naive": 128,
        "expected_optimized": 1
      },
      {
        "name": "achieved_tflops_pct_of_peak_bf16",
        "description": "Achieved TFLOPS as a percentage of v5e peak bf16 TFLOPS (197.0). Secondary signal; this kernel is not compute-bound.",
        "direction": "higher_is_better"
      }
    ]
  }
}
